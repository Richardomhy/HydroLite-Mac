name: demo_variant

model:
  time_step_hours: 1.0

inputs:
  directory: data_demo
  rainfall: rainfall.csv
  subcatchments: subcatchments.csv
  reaches: reaches_variant.csv

outputs:
  directory: output/demo_variant

