# GitHub Push Commands

```bash
git status
git remote -v
git remote add origin <你的GitHub仓库URL>
git branch -M main
git push -u origin main
```

Replace `<你的GitHub仓库URL>` with the HTTPS or SSH URL of your own GitHub repository.
