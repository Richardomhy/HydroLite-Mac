# RUSLE Factors

Every factor needs source, unit, CRS, resolution, nodata and confidence. Missing factors stop the calculation rather than defaulting to one. LS needs projected metre-based DEM inputs.
