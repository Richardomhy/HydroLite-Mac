# NASA Earthdata Connector

The connector detects `earthaccess` and authentication without reading or printing credentials. Search requires bbox and dates, limits results to 20 granules, and registers ICESat-2, GPM, SMAP, MODIS and elevation sources.

HDF5 is not downloaded by default. Credentials remain in supported user-level authentication outside Git.
