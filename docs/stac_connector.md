# STAC Connector

STAC search is limited to configured trusted catalogs or a user-confirmed endpoint. Every query requires collection, bbox and datetime. Preview and size estimates precede any small-file download.

Earth Search and Microsoft Planetary Computer are configured. Unknown endpoints are not trusted automatically.
