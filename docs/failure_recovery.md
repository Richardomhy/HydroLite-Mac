# 失败恢复

运行中心区分输入、依赖、认证、后端、timeout、取消、数值稳定性、输出缺失、权限、磁盘和内部错误。

- 默认最大尝试次数为 1；网络型连接器最多可配置为 2。
- 取消只终止当前任务记录的进程组。
- completed artifacts 始终保留。
- 使用 `runs retry-stage` 从失败阶段重新入队。
- 使用 `runtime recover` 固化中断状态和恢复建议。

不得把失败任务改写为成功。
