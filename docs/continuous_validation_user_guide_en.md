# Continuous Validation Guide

Run the input, PET, flow, balance, truth and structure commands. Review `output/continuous_validation/summary/`. Synthetic recovery verifies code consistency only; use independent observed data for project validation.
