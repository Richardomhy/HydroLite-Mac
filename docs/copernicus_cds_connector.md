# Copernicus CDS Connector

The connector detects `cdsapi` and only checks whether `~/.cdsapirc` exists. ERA5/ERA5-Land plans require variables, bbox, dates, resolution and format. Requests are not executed by default.

NetCDF standardization requires optional data dependencies.
