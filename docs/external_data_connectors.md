# External Data Connectors

The registry provides GEE, NASA Earthdata, Copernicus CDS, approved STAC and local-file adapters. Each exposes dependency/authentication status, supported datasets, bounded search/preview, estimation, explicit execution, export, cancellation and healthcheck.

Downloads default to false. Remote searches require bbox and dates. Credentials are redacted and never copied into the workspace or repository.
