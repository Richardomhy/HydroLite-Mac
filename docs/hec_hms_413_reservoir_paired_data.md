# HEC-HMS 4.13 Reservoir Paired Data

本机 4.13 `river_bend` 官方样例确认 `.pdata` 的 `Paired Data Manager` 和 `Table` 结构，并包含 `Elevation-Storage` 表与受控出流水库。该样例未提供经验证的 `Storage-Discharge` Outflow Curve 语义，因此原创项目 compute 保持门禁失败。
