# 连续模型验证使用指南

运行 `python -m hydrolite continuous audit-inputs data_demo/drought`、`audit-pet`、`truth-generate`、`truth-forward` 和 `truth-recover`。查看 `output/continuous_validation/summary/`。真实项目需先提供带单位和时间戳的观测流量。
