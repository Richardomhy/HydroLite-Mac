# 洪水预测集合

当前集合是 `scenario_ensemble`，用于回答“不同降雨假设下模型可能产生什么结果”。它不是经过气象概率权重校准的正式概率集合。

输出保留每个成员的完整过程线，并计算 p05、p25、p50、p75、p95、峰值、峰现时间、体积、库水位和库容分布。超阈结果使用 `scenario_member_exceedance_fraction`，不自动生成蓝、黄、橙、红法定预警。

不确定性来源分为降雨输入、水文参数、模型结构、水库曲线、初始条件、数据驱动残差和 unresolved。
